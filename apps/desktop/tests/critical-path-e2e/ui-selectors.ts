/** Scoped selectors for non-text controls used by the Electron UI driver. */

/** Icon-only directory-browser button, distinct from its path textbox. */
export const EDIT_PATH_BUTTON_SELECTOR = './/button[@aria-label="Edit path"]'

/** Sidebar tab root, including its title, badge, and nested close button. */
export const TASKS_TAB_SELECTOR = './/div[@title="Tasks"]'
