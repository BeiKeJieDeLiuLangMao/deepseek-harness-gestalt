- id: member-question-receiver
  config:
    storagePath: !!js dshHomePath('member-question-receiver')
    environment: development
    maxRecords: 1000
    terminalRetryMs: 1000
    terminalAuthorityMode: development-local
    memberQuestionInstallationId: receiver-installation
    memberQuestionDeviceName: Receiver Desktop
