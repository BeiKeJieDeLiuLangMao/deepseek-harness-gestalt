# Optional CLIProxyAPI delegation profile

English | [中文](delegation-routing-cliproxyapi.zh.md)

For provider-neutral dispatch and owner reuse, use [delegation routing and context reuse](delegation-routing.md). For the current delivery-role priority chain, exclusions, pool handling, and model-change rules, use the delivery skill's [CLIProxyAPI model-routing authority](../../.agents/skills/orchestrate-dsh-delivery/references/model-routing-cliproxyapi.md).

Both references depend on the live catalog and verified capabilities. They do not define static account counts, inspect credentials, implement product routing or failover, or authorize the root session to change its own model.
