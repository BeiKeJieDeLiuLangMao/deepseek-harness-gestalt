# Local modifications

Every divergence from the pinned upstream commit in [UPSTREAM.md](UPSTREAM.md). Product behavior belongs in `@deepseek-ai/dsh-client-ui-workbench`, not here.

1. **Workspace package manifest** — `package.json` / `tsconfig.json` / `tsconfig.dts.json` / this file / `UPSTREAM.md` are repository-owned. Upstream `package.json` name `dsh-better-sidebar` is `@deepseek-ai/dsh-client-ui-better-sidebar`; the upstream plugin id, Cordis name, and settings namespace stay `dsh-better-sidebar`, and the workspace version follows the monorepo root. The manifest maps upstream runtime dependencies to workspace peers and development dependencies, while `react-icons` remains a bundled dependency. `tsconfig.json` project references use this repository's package paths (`../../core/session`, not `../../session/session`). The snapshot is not a `tsconfig.client.json` project: those references would pull host `Context` merges into the client program.
2. **`src/config.ts`** — `import z from 'schemastery'` is `import z from '@deepseek-ai/schemastery'`.
3. **`src/context-types.ts` / `src/client/context.ts` / `src/client/index.ts`** — the upstream `CordisContext & SidebarContextShape` intersection is named `SidebarContext`, while the public Client entry alone carries the `betterSidebar` Cordis augmentation. The JSX-free upstream `index.tsx` entry is named `index.ts` so its source name matches the public `index.d.ts` compiler face. This repository adds the concrete client connection and Session-admission types plus the provisional Session and `uiRenderer` faces required by the canonical Side Chat composition. `SidebarSessionEvent.data` is `unknown` so Host `SessionEvent` payloads such as `UserMessage` assign into the mirror. `stageProvisional` takes branded `SessionId`.
25. **`src/client/css-modules.d.ts`** — Host Typert analysis of this package includes the Client half. `css-modules.d.ts` declares `*.module.css` and `@xterm/xterm/css/xterm.css`. There is no global `*.css` wildcard. The unused snapshot `layout.css` side-effect sheet is omitted.
4. **`src/invariant.ts` / `src/context-types.ts` invariant service face** — omitted because this adapter has no independently published second observation; the package README records the omission reason.
5. **`tsdown.config.ts` / `src/client/chunk-loader.ts`** — client factory id is the workspace package name; Host/Client build faces split the Node library from the browser chunks; the plugin-registry `client-registry.js` channel is omitted; chunk and client externals request `@deepseek-ai/cordis`. The Node library build emits only snapshot sources to `lib/types` through `tsconfig.dts.json` (`noCheck` and `noResolve`) so published declarations exist without joining the client aggregate or emitting dependency declarations beside dependency sources.
6. **`src/bundle-route.ts`** — `LIB_DIR` is the package `lib/` directory, not `dirname(import.meta.url)`. Source launch (`tsx`) otherwise looks for `src/client-terminal.js` and the terminal / editor / mermaid chunks 404.
7. **`src/client/BrowserView.tsx`** — when `ctx.get('workbenchBrowser')` is published, the tab renders that official chrome. The sandboxed iframe remains the fallback for a standalone snapshot install.
8. **`src/client/service.ts`** — `setPanelOpen(open)` expands or collapses the right workbench panel. The official preview and first Agent tab use it; a type-only `openTab` does not expand. `createTab` receives the `openTab` seed so a descriptor can reuse an explicit id, title, or meta instead of minting a fresh identity.
9. **Official Desktop overlay chrome** — under `window.dshDesktop.chromeOverlayShow`, official Sidebar `+` menus open in the Desktop native overlay view. Overlay Settings still use the Host chrome settings seat. The unused snapshot `TabBar` / `Sidebar` shell is omitted; official DockKit owns tab chrome.
10. **`src/client/state.ts` / `src/client/sidebar.module.css`** — snapshot store types and viewer CSS remain for official file/runtime/Changes/Side Chat bodies. Panel-push CSS, automatic Files home, and the unused snapshot shell are omitted.
11. **`src/client/index.ts`** — the Desktop overlay document (`data-dsh-desktop-overlay` / `?dsh-desktop-overlay=1`) registers official adapters without Host-open routing or Side Chat admission. It never mounts a snapshot `Sidebar` into `document.body`. Interactive Host documents intercept `sessions.open` / `sessions.openSubagent` so a `Side: ` catalog child opens or focuses its official Side Chat tab on the ordinary owner.
12. **`src/sidechat-routes.ts` / `src/index.ts`** — live Side Chat Agent handles and pending snapshots belong to one plugin activation. Teardown closes route admission, waits for admitted calls, clears pending snapshots, and releases every handle before the plugin becomes quiescent.
13. **`src/client/SideChatView.tsx` / `src/client/index.ts` / `src/client/api.ts` / `src/client/subagent-detect.ts` / `src/context-types.ts` / `src/sidechat-core.ts` / `src/sidechat-routes.ts`** — the Side Chat tab keeps only child creation and lifecycle. It stages a provisional renderer identity, mounts the repository `main.conversation` content entry through `uiRenderer.mountSession()`, and creates the Host Agent and Session under the same id only when the first message is submitted. The provisional row stays out of subagent catalogs and counts. That first admission captures parent history, installs the provisional model and permission choices, and publishes the child. The Session constructor receives exactly the inherited prefix; setup appends the child descriptor as the first owned event. A runtime admission adapter routes prompt/cancel, queue/steer, permission, skill-catalog, and model operations while hiding the inherited seed prefix. The snapshot's thread switch, promotion toolbar, transcript polling, message renderer, composer, header-info route, connection mirror, related CSS, and unused locale strings are absent. The compact header omits Session navigation and the preset label while the current child catalog, jobs, and schedules resolve against the child id. Descendant selection retargets the explicit renderer without changing shell selection, and the tab retains its root Side Chat id for lifecycle cleanup; workbench terminals retain their own scope.
14. **`src/git.ts`** — an explicit child-repository selection that is absent from the current discovery result fails instead of falling back to the first repository and applying a mutation to the wrong checkout.
15. **`src/agent-opens.ts`** — `sidebar_open` delivery isolates failed WebSocket senders, detaches them, and retains each request until at least one connected view accepts it.
16. **`src/loopback-allowlist.ts` / `src/index.ts` / `src/client/browser.ts` / `src/client/BrowserView.tsx` / `src/prefs-shared.ts`** — Host and Client share one loopback allowlist parser. Security prose distinguishes the opaque default sandbox from the own-origin privileges granted only to explicitly allowlisted loopback pages.
17. **`src/client/locales-zh-HK.ts` / `src/client/locales-zh-MO.ts` / `src/client/locales-zh-TW.ts` / `src/client/locales.ts`** — regional Traditional Chinese dictionaries use descriptive export names so the repository's zh/en fallback parity gate does not mistake them for incomplete fallback pairs. The Macao and Taiwan module descriptions name their actual locale.
18. **`src/client/intercept.tsx`** — disabling chat file-open interception also leaves the produced-files turn tail with the official `ui-deliverables` owner; sidebar chips never bypass the setting.
19. **`src/client/state.ts` / `src/client/sidechat-restore.ts` / `src/client/service.ts` / `src/client/subagent-detect.ts` / `src/client/builtins/tabs.tsx` / `src/client/api.ts` / `src/sidechat-core.ts` / `src/client/SideChatView.tsx` / `src/sidechat-routes.ts`** — Side Chat tabs restore from durable Session data after the origin-scoped strip is lost. Apply-lifetime projection subscriptions restore published, unarchived direct children without replacing the active tab. The Host serializes close with first publication and reports durable identity; close waits for release and archive before removing the tab, and client teardown prevents late commits. A local tombstone covers projection delay. `?dsh-sidebar-reset` skips restoration, and shared core helpers own root-thread identity.
20. **`src/client/intercept.tsx` / `src/client/sidebar.module.css`** — the produced-files "show in folder" action carries a dedicated `producedFolder` class that resets the native button border and background; sharing the `+N` counter's `producedMore` class left the browser's default button chrome visible.
21. **`src/path-security.ts` / `src/index.ts` / `src/html-route.ts`** — read routes (`fs.read`, `/sidebar/file`, `/sidebar/html`) resolve existing files through `resolveReadablePath` without workspace containment, so an explicitly opened editor tab, its markdown images, and its HTML preview work for files outside the session workspace. Write routes (`fs.write`, upload), `fs.tree`, and `fs.search` keep the containment guard.

22. **`src/client/sidebar.module.css`** — right and bottom workbench panels use `contain: style` only. `contain: layout` shifted an absolutely positioned Desktop panel so the tab strip sat off-screen and the official page covered the sidebar tab list.
23. **`src/client/service.ts`** — an `openTab` seed with a path or URL whose active pane is in the bottom tree lands in the right workbench before minting. Type-only `+` clicks still follow the pane that owns the menu.
24. **`src/client/state.ts`** — the snapshot store still collapses a tree that lost its last docked tab. Official `ui-sidebar-right` owns the production close path; the unused snapshot shell no longer mounts.
26. **`src/client/official-files/*` / `src/client/official-open-routing.tsx`** — official file addresses always retain the Session that owns the resource and its filesystem authority. An embedded child can select its parent's visible official workbench as the display host while reads, writes, viewer loads, tree operations, references, produced-file actions, and conversation insertion continue to use the child Session.

## 0.18.0 refresh dispositions (`f9153dfc` → `f59ffd07`)

Allowed import paths only: `dsh.plugin.json`, `src`, `tsdown.config.ts`. Upstream `src/client/sidechat-transcript.ts` was **not** imported (retired parallel transcript/polling renderer). Locale dictionaries stay in sibling files; the new `src/client/chunks/locale.tsx` lazy chunk is **adopted**, and `CHUNKS` includes `locale`. Split-sidebar modules under `src/client/changes/` are **adopted**. The unused snapshot `src/client/sidebar/` shell is omitted.

| LOCAL row | Disposition |
|---|---|
| 1 workspace manifests | **retained** — still repository-owned; not in the allowed import paths |
| 2 `src/config.ts` schemastery | **retained** |
| 3 `SidebarContext` / Client augmentation / admission / `uiRenderer` / workspaces archive | **retained**; the sole `betterSidebar` Cordis augmentation is **relocated** to `src/client/context.ts`, and `openWorkspacePath` is **relocated** onto `remote.session`. `SessionAdmissionAdapter` type-imports `@deepseek-ai/dsh-api-session-controller/client`. `registerAdmissionAdapter` is required. `SidebarSessionEvent.data` is `unknown`; `stageProvisional` takes branded `SessionId`. |
| 25 Host-reachable CSS ambient | **retained as `css-modules.d.ts` only** — `*.module.css` plus `@xterm/xterm/css/xterm.css`; unused snapshot `layout.css` omitted |
| 4 `src/invariant.ts` / `SidebarContextShape.invariants` | **omitted** — this adapter has no independently published second observation |
| 5 tsdown / chunk-loader workspace factory id | **retained** (`clientBundle('@deepseek-ai/dsh-client-ui-better-sidebar', 'client.js')`). `dsh.plugin.json` still names `./lib/client-registry.js` as the upstream plugin-registry channel; this repository's tsdown face still omits emitting that extra factory. Locale is an additional chunk, not a second registry channel. |
| 6 `src/bundle-route.ts` `LIB_DIR` | **retained** (package `lib/`, including `client-locale.js`) |
| 7 BrowserView workbenchBrowser | **retained** |
| 8 `setPanelOpen` / `createTab` seed | **retained** |
| 9 Desktop overlay `+` menu / windowChrome | **relocated** to official Sidebar / DockKit; snapshot `TabBar` omitted |
| 10 panel resize / no automatic Files home | **omitted** with the unused snapshot shell; store types and viewer CSS remain |
| 11 Desktop overlay does not mount Sidebar | **retained** — overlay still skips Host-open routing, Side Chat admission, and `Side: ` catalog interception |
| 12 Side Chat Agent-handle quiescence | **retained** |
| 13 canonical Conversation Side Chat | **retained**. Upstream in-tab composer, history menu, transcript polling, soft-close, and `sidechat-transcript` stay **retired**. |
| 14 git selected-repo fail-loud | **retained** |
| 15 `sidebar_open` sender isolation | **retained** |
| 16 shared loopback allowlist | **retained** (0.18 `fenceEnabledOf` adopted for write/tree/search; loopback parser stays the shared module) |
| 17 regional zh export names | **retained**; locale chunk aliases those descriptive exports |
| 18 intercept produced-files vs deliverables | **retained** |
| 19 restore/close / `?dsh-sidebar-reset` | **retained** |
| 20 `producedFolder` class | **retained** |
| 21 `resolveReadablePath` for reads | **retained**; write/tree/search still use workspace fence |
| 22 `contain: style` on docked panels | **retained** |
| 23 bottom-tree `openTab` seed lands right | **retained** |
| 24 last docked tab collapses panel | **relocated** to official `ui-sidebar-right`; snapshot-store tests omitted |

## 0.18.1 main refresh dispositions (`f59ffd07` → `d88dcfc3`)

All existing rows remain retained. The source delta adopts file-tree rename/delete, changes-preview reading and redaction, diff expansion, submenu positioning, quoted terminal settings, and regex terminal waits. The repository-owned manifest, tests, and build faces remain authoritative.

The upstream Side Chat seed marker is already represented by canonical admission. `isSeeded` remains true and `inheritedEventCount` ends at `inheritance.seed.length`: the appended child descriptor is child-owned. Its model selection, renderer admission, persistence handles, and close lifecycle remain intact. Desktop overlay menus, window drag space, phone tab badges, and the request-trust adapter are retained. Regional locale exports and `saveShortcutTitle` remain repository-owned. The new `src/client/tree-mutations.ts` uses the existing `SidebarContext` mirror instead of the upstream `Context` name.
