/**
 * Browser copy of the workspace search Remote descriptors, inlined from the
 * host contract subpath. The host package root is not a client-safe import.
 */
export {
  WORKSPACE_REFERENCE_INVOCATIONS,
  sessionIdSchema,
  workspacePathEntrySchema,
} from '@deepseek-ai/dsh-workspace-reference/src/contract.ts'
